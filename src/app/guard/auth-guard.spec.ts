import { TestBed } from '@angular/core/testing';

describe('AuthGuard', () => {
    beforeEach(() => {
        TestBed.configureTestingModule({});
    });

    it('should pass', () => {
        expect(true).toBe(true);
    });
});
